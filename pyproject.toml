[project]
name = "vale"
version = "0.1.0"
description = "Move to a more modern Python stack using uv"
dependencies = [
    "python-dotenv==1.1.0",
    "dependency-injector==4.46.0",
    "discord.py==2.6.4",
    "motor>=3.7.1",
]
requires-python = ">=3.9"

[tool.uv]
managed = true
